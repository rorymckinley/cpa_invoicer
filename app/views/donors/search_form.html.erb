<h1>Donor Search</h1>

<%= form_for(@donor, method: :get) do |f| %>
  <%= f.label :donor_no, 'Donor No' %>
  <%= f.text_field :donor_no %><br />
  <%= f.submit %>
<% end %>

<%= link_to "Back to Menu", root_path %>
